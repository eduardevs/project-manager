{% if form %}
	{{ form_start(form) }}
		<div class="p-3">
			<div class="mt-2">
				{{ form_row(form.q, {'attr': {'class': 'form-control w-75 mx-3'}}) }}
				{{ form_errors(form.q) }}
			</div>
			<div class="">
				{{ form_label(form.categories, 'Categories', {'attr': {'class': 'w-25 text-uppercase'}}) }}
				{{ form_row(form.categories, {'attr': {'class': 'mx-4'}}) }}
			</div>
			<div class="d-flex justify-content-center">
				<button class="btn btn-secondary custom-btn text-uppercase fw-bold mt-2" type="submit" id="button">
					filter
					<i class="fa-solid fa-magnifying-glass mx-2"></i>
				</button>
			</div>
		</div>
	
	{{ form_end(form) }}
{% endif %}

